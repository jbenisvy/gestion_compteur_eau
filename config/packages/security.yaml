security:
  password_hashers:
    Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

  providers:
    app_user_provider:
      entity:
        class: App\Entity\User
        property: email

  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false

    main:
      lazy: true
      provider: app_user_provider
      custom_authenticators:
        - App\Security\LoginFormAuthenticator
      login_link:
        check_route: app_login_check
        login_path: app_login
        signature_properties: [id, email, updatedAt]
        max_uses: 5
        lifetime: 1800
      logout:
        path: app_logout
        target: home

  access_control:
    - { path: ^/login, roles: PUBLIC_ACCESS }
    - { path: ^/verify/email, roles: PUBLIC_ACCESS }
    - { path: ^/admin/login, roles: PUBLIC_ACCESS }
    - { path: ^/admin/evenements, allow_if: "is_granted('ROLE_ADMIN') or is_granted('ROLE_SYNDIC')" }
    - { path: ^/admin/facturation, allow_if: "is_granted('ROLE_ADMIN') or is_granted('ROLE_SYNDIC')" }
    - { path: ^/admin/stats, allow_if: "is_granted('ROLE_ADMIN') or is_granted('ROLE_SYNDIC')" }
    - { path: ^/admin, roles: ROLE_ADMIN }
    - { path: ^/syndic, roles: ROLE_SYNDIC }
    - { path: ^/compte, allow_if: "is_granted('ROLE_ADMIN') or is_granted('ROLE_USER') or is_granted('ROLE_SYNDIC')" }
    - { path: ^/(dashboard|historique|saisie), roles: ROLE_USER }
    - { path: ^/, roles: PUBLIC_ACCESS }
